/**
 * @returns simple text
 */
function DragDropText() {
    return (
        <div className="text-center">
            <p className="drag-drop-text">Drag and drop to reorder list</p>
        </div>
    )
};

export default DragDropText;
